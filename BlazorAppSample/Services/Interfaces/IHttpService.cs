using Newtonsoft.Json.Linq;

namespace BlazorAppSample.Services.Interfaces
{
    public interface IHttpService
    {
        //Task<JObject> GetHttpResponse(HttpMethod type, string url, string body);
       // Task<JObject> GetResponse<T>(T model, string url, HttpMethod type);
    }
}
